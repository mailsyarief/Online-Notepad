<?php

$host = "localhost";
$user = "root";
$password = "";
$dbname = "simple_crud_flutter";

$conn = mysqli_connect($host, $user, $password, $dbname);

if (!$conn) {
	die("error in connection");
}else{
	
}
?>